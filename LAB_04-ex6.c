#include <stdio.h>
#include <math.h>

float somatoria(int n){
    
    // Não consegui realizar a lógica da funcão de somatória

}

int main() {
    int n;
    printf("Digite o valor de N: ");
    scanf("%d", &n);

    float soma = somatoria(n);
    printf("O resultado da somatória é aproximadamente: %13.f\n", soma);

    return 0;
}